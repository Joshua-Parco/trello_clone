<%= link_to "Back to Board", board_lists_path(@list.board_id, @list) %>
<h1><%= @list.name %></h1>
<%= link_to 'Add Task', new_list_task_path(@list) %>
<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.name, [@list, task] %></li>
  <% end %>
</ul>
